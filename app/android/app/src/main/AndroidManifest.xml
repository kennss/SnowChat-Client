<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>

    <!-- Phase 10: AI 에이전틱 도구 권한 -->
    <uses-permission android:name="android.permission.READ_CALENDAR"/>
    <uses-permission android:name="android.permission.WRITE_CALENDAR"/>
    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.READ_SMS"/>
    <uses-permission android:name="android.permission.READ_CALL_LOG"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" tools:ignore="ProtectedPermissions"/>
    <uses-permission android:name="android.permission.SET_ALARM"/>

    <!-- Phase 8.2 VoIP — WebRTC 음성 통화 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30"/>
    <!-- Android 14+ AppOps RECORD_AUDIO 강화 대응 — Phase E 에서 foregroundServiceType=microphone으로 실제 런치 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE"/>
    <!-- Android 9+ telecom ConnectionService (Self-Managed, includesCallsInRecents=false 해당 없음) -->
    <uses-permission android:name="android.permission.MANAGE_OWN_CALLS"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <application
        android:label="snowchat"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:localeConfig="@xml/locales_config"><!-- 2026-05-08 i18n locale lock (Phase 0). Pairs with res/xml/locales_config.xml + build.gradle resourceConfigurations + MainActivity.attachBaseContext. Documented in Documentation/TO-DO/2026-05-07-app-locale-and-i18n.md. -->
        <!--
             Option 2 (dedicated CallActivity hosting a cached FlutterEngine)
             attempted 2026-05-06 and reverted same day: FlutterFragment
             exclusively locks an attached engine and refuses to let a
             second Activity attach to it (AssertionError: "internal
             FlutterEngine has been attached to by another activity"). With
             a single Dart isolate hosting the WebRTC peer connection there
             is no way to render /call in a separate Activity without a
             native Foreground Service rewrite — out of scope for now.

             So MainActivity is once again the call host. The keyguard-bypass
             attributes here are scoped to the call path by Dart-side route
             logic: /call is the only route pushed via the Accept flow, and
             when it ends with the keyguard still locked we run
             SystemNavigator.pop() to finish this Activity instead of
             popping into the underlying chat/wallet route. That post-call
             finish closes the only meaningful security gap of this setup.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!--
                 Phase 1 invite deeplink (snowchat://invite/<code>).
                 BROWSABLE 없으면 외부 메신저(카톡 등)에서 클릭한 링크가 못 들어옴.
                 DEFAULT 없으면 OS 가 자동 분기 시 후보에서 제외.
                 host="invite" path 는 와일드카드 — code 자체는 router 에서 파싱.
                 Phase 2 = https://snowchat.calidalab.ai/invite/* (autoVerify=true).
            -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="snowchat" android:host="invite"/>
            </intent-filter>
        </activity>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
